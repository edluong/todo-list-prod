doctype html
html 
    head
        meta(charset='utf-8')
        title To Do List
        link(href="https://fonts.googleapis.com/css?family=Amaranth" rel="stylesheet") 
        link(rel='stylesheet',type="text/css",href="/style.css")
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
    
    body
        div(class='container')
            div(class='subcontainer')
                p 
                    h1 Add Item
                //-Make sure to indent the input or button. Or it would be outside of the nested form tags
                form(action='/addtask',method='post')
                    input(type='text',name='newtask',placeholder="Add New Task") 
                    button Add Task
                    button(type="reset") Reset Task Field
                
                    h1 Added Task  
                    ul
                        each val in task
                            li 
                                input(type="checkbox",name="check",value=`${val}`)
                                label(for=`${val}`) #{val}
                    |
                    h2 
                    |
                    button(formaction='/completedtask',action='submit') Complete Task
                    h1 Completed Task
                div(class='completed')
                    p 
                        ul
                            each val in completed
                                li #{val}
            p To Do List by 
                 a(href='http://www.edmundluong.com') Ed Luong
            p
            | Technologies: AWS RDS & EC2, Node.JS, Express.JS, PostGresSQL 
            p
            | Check out the code at 
            a(href='https://github.com/edluong/node-to-do-list') Github! 
                       
